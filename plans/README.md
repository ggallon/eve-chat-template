# Implementation Plans

Generated by the improve skill on 2026-06-27; reconciled 2026-06-28 against
`1b9c9ac`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Split `components/chat/message.tsx` into a `message/` sub-component module | P2 | M | — | DONE (PR #1, verified 2026-06-28: 9 files present, old file gone) |
| 002  | Fail fast when `BETTER_AUTH_SECRET` is unset | P1 | S | — | DONE (merged via PR #3 / `1b9c9ac`; verified 2026-06-28: fallback string gone, `throw` at `lib/auth.ts:14`) |
| 003  | Fail loud when rate-limit Redis is unconfigured | P1 | S | — | DONE (executed 2026-06-28 against `a8dc2b7`: silent `return` → `throw new Error(...)` at `lib/rate-limit.ts:51-55`; `pnpm typecheck` exit 0, no new lint errors in file; change staged on `advisor/003-fail-loud-rate-limit` — commit pending SSH signing-key passphrase) |
| 004  | Re-point stale `components/chat/message.tsx` references in the docs | P2 | S | — | TODO (no drift since `d1daea6`; all 3 stale refs still present at lines 41/714/831) |
| 005  | Add a test runner and the first batch of characterization tests | P1 | L | — | TODO (no drift since `d1daea6`; runner absent, all 4 target modules present & pure) |
| 006  | Repair the broken `pnpm check` lint gate | P2 | M | ~~005~~ | **DRIFTED — see 006 note below; bucket C removed, 005 dep drops** |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **001 → DONE**: executed by PR #1 (`Split chat message into sub-components`).
  The split is in the tree (`components/chat/message/` is a 9-file module);
  plan 004 fixes the stale docs that the split left behind.
- **004 before any doc rewrite** of `docs/how-the-chatbot-works.md`: 004 fixes
  the small stale `message.tsx` references; a broader docs cleanup (the
  "Setup Readiness" / `lib/setup.ts` section describing a subsystem that
  doesn't exist, and the "only Notion" connections-menu drift) is tracked
  as findings but NOT planned yet — coordinate so the large rewrite doesn't
  collide with 004's three-line edits.
- **005 → 006 (REVISED 2026-06-28)**: plan 006 originally depended on 005
  because its Bucket C fixed the `session-chat-page.tsx` resume-effect
  `useExhaustiveDependencies` + `noExcessiveCognitiveComplexity` violations,
  which were unsafe to auto-fix before the resume behavior was pinned by
  tests. **Reconciliation found those 5+1 violations no longer fire**
  (`session-chat-page.tsx` is byte-identical to `d1daea6`, biome/ultracite
  versions unchanged, biome.jsonc unchanged — the rules silently stopped
  triggering; cause unknown, worth a separate look). With Bucket C empty,
  006 no longer needs 005 as a safety net for the resume effect. 005 is still
  valuable on its own merits (zero tests exist) and remains a prerequisite for
  a *later* "characterize the resume effect" follow-up, but 006 can now proceed
  independently. The 005→006 ordering in the table is therefore relaxed.
- **002 and 003 are independent and highest urgency** — both security, both
  S effort, no dependency. They can be done in parallel. 002 is DONE; 003 is
  the remaining top-priority item.
- **Perf findings (F10/F11/F12: `AgentMessage` memoization, per-event
  ownership-SELECT redundancy, O(n²) event-log dedup)** touch the streaming
  hot path in `agent-chat.tsx` and benefit from 005's tests existing first —
  not planned in this batch.

## Reconciliation log (2026-06-28, against `1b9c9ac`)

- **001 verified DONE**: `ls components/chat/message/` shows all 9 files;
  `components/chat/message.tsx` deleted. No regression.
- **002 verified DONE**: `rg "eve-chat-template-unconfigured-secret" lib` →
  no matches; `rg "throw new Error" lib/auth.ts` → one match at `lib/auth.ts:14`.
  Merged via PR #3 (`1b9c9ac`); index previously cited an intermediate branch
  commit, corrected above.
- **003–005 unchanged TODO**: drift check `git diff --stat d1daea6..HEAD` on
  each plan's in-scope files returned empty for all three. Findings re-confirmed
  live: 003's silent `return` at `lib/rate-limit.ts:51-53`; 004's three stale
  `message.tsx` doc refs at lines 41/714/831; 005's runner absent and all four
  target modules present & pure (`lib/chat/events.ts` settled set
  `session.completed`/`session.failed`/`session.waiting`; `lib/chat/title.ts`,
  `lib/chat/limits.ts`, `components/chat/message/tool-status.ts` unchanged).
  `Planned at` SHAs still accurate — no refresh needed.
- **006 DRIFTED (Bucket C gone, Bucket set changed)**: re-ran `pnpm check`
  against `1b9c9ac` — **18 errors**, down from ~21 at planning time. The
  highest-risk subset (Bucket C: 5× `useExhaustiveDependencies` +
  1× `noExcessiveCognitiveComplexity` on `session-chat-page.tsx:73,205,277,308`)
  **no longer fires**. `session-chat-page.tsx` is byte-identical to `d1daea6`
  (`git diff` empty), `biome.jsonc` unchanged, `@biomejs/biome@2.5.1` and
  `ultracite@7.8.3` unchanged — so the rules stopped triggering without any
  config/version bump. Cause unknown; flagging as a separate item below.
  Consequence: **006's Bucket C is empty**; the 005 dependency was for Bucket C
  only, so 005→006 is relaxed (see Dependency notes). 006's Buckets A/B/D
  largely still apply, but the current 18-error set also differs from 006's
  cited set in ways the executor must re-derive at Step 1:
  - **New errors not anticipated by 006**: `components/chat/composer.tsx`
    (3: `useAriaPropsSupportedByRole`, `noNoninteractiveTabindex`,
    `noNestedTernary`), `agent-chat-shell.tsx:100` (`noDocumentCookie`),
    `agent-chat-shell.tsx:404` (`noDangerouslySetInnerHtml` — 006 listed
    agent-chat-shell.tsx but only for 3 a11y errors at line 355, not this),
    `sign-in-button.tsx:65` (`useOptionalChain` — 006 listed sign-in-button.tsx
    in-scope but didn't cite this rule).
  - **Errors 006 cited that still match**: `auth-display.tsx:36` +
    `app/layout.tsx:103` (`noDangerouslySetInnerHtml`, Bucket D), `user-menu.tsx:92`
    (`noImgElement`+`useImageSize`, Bucket B), `app/api/auth/[...all]/route.ts`
    (`useAwait`×2, `noUnusedVariables`, `useOptionalChain` — note 006 cited
    the route's `useOptionalChain` but Step 1 now shows it on
    `sign-in-button.tsx:65` instead; re-read both), `home-chat-page.tsx:61,65`
    (`useExhaustiveDependencies` — STILL fire, just 2 not 5; and
    `home-chat-page.tsx:138` `noEmptyBlockStatements`), `agent-chat-shell.tsx:355`
    (3 a11y), `agent/tools/get_weather.ts:9` (`useAwait`).
  - **Action for 006**: the plan's Step 1 ("re-audit the current error list,
    trust Step 1 over the plan's numbers") is exactly the right instruction and
    survives the drift. But the plan's Step 5 (Bucket C) and the 005 dependency
    are stale. **Refresh 006 in place** before executing: drop Step 5, drop the
    005 dependency in the Status block, and add the new files
    (`composer.tsx`, the `agent-chat-shell.tsx:100/404` lines,
    `sign-in-button.tsx:65`) to the in-scope list with the same bucketing
    instinct (a11y/`noDocumentCookie`/`noNestedTernary` are low-risk; the new
    `noDangerouslySetInnerHtml` at `agent-chat-shell.tsx:404` goes into
    Bucket D and needs the same static-vs-runtime review).
- **New finding surfaced during reconcile**: the silent disappearance of
  `session-chat-page.tsx`'s resume-effect lint violations, with no config or
  version change, is itself suspicious. Either (a) an ultracite rule default
  changed out-of-band, (b) a transitive biome plugin update shifted, or
  (c) the resume effect was genuinely refactored in a way `git diff` against
  `d1daea6` doesn't capture (e.g. an upstream `eve/react` type change altered
  what `useExhaustiveDependencies` sees). Not blocking, but worth a
  `pnpm update`/lockfile-diff check before trusting the gate. Filed below in
  "considered and rejected" as a *watch item*, not a new plan.

## Notes

- This set was produced by a full audit (Phase 1 recon + parallel category
  audits + vetting against the cited code). Planned at commit `d1daea6` on
  the `canary` branch; reconciled against `1b9c9ac` on 2026-06-28.
- Repo verification baseline at planning time (`d1daea6`): `pnpm typecheck`
  exits 0; `pnpm check` exits 1 with ~21 errors (pre-existing — that's plan
  006); no `pnpm test` runner exists (plan 005 adds it).
- **Current baseline at reconciliation (`1b9c9ac`)**: `pnpm typecheck` exits 0;
  `pnpm check` exits 1 with **18 errors** (down from ~21 — see Reconciliation
  log for the delta); still no test runner. Both baselines consistent with the
  plan set; 006's Step 1 re-audit instruction absorbs the count change.
- All plans inline the conventions, excerpts, and commands the executor
  needs; no plan references "the audit" or "the discussion."

## Findings considered and rejected (so nobody re-audits them)

- **WATCH (not a plan): `session-chat-page.tsx` resume-effect lint violations
  silently stopped firing between `d1daea6` and `1b9c9ac`** with no
  `biome.jsonc`, `@biomejs/biome`, or `ultracite` change and the file
  byte-identical. Cause unexplained (possible transitive plugin/lockfile
  drift, or upstream `eve/react` type effect on
  `useExhaustiveDependencies`). Not blocking — the violations disappearing is
  net-positive — but before adding `pnpm check` to CI (the follow-up 006
  enables), do a `pnpm install --frozen-lockfile` + `pnpm update --dry-run`
  diff and a `git log -- pnpm-lock.yaml` check to confirm what shifted. If a
  rule genuinely relaxed, the resume-effect code is now unguarded by lint and
  the "characterize the resume effect" follow-up (originally 005's successor)
  becomes more valuable, not less.

- **CSP / security headers** (next.config.ts has no `headers()`): MED
  confidence, could not confirm whether Next.js 16 already ships a baseline
  CSP; the chat surface is same-origin. Not worth a plan at this depth —
  revisit if Streamdown sanitization is ever in question.
- **`incr`/`expire` race in `lib/rate-limit.ts`** (a crash between `incr`
  and `expire` leaves a rate-limit key with no TTL): real but separate from
  plan 003; file as its own finding later, don't fold into 003.
- **postcss transitive `<8.5.10` (GHSA-qx2v-qp2m-jg93, moderate XSS)** via
  next 16.2.9's pinned postcss: low-impact (build-time), fixable via
  `pnpm.overrides`, but needs a `next build` + visual smoke to verify. Not
  planned this batch — a one-line `pnpm.overrides` is a small follow-up.
- **Dead shiki direct deps** (`@shikijs/*` + `shiki` v4 declared, never
  imported; streamdown pulls v3 transitively): LOW impact, S effort. Not
  planned this batch but trivially removable — a quick win for the next run.
- **Three divergent "owned chat" guard patterns** in `lib/db/queries.ts`
  (4 sites, drift between throw and return-null): LOW-MED tech-debt, S
  refactor. Not planned; fold into the eventual `lib/db/queries.ts`
  test+refactor pass.
- **`agent-chat.tsx` 2192-LOC god-module**: real, HIGH confidence, but L
  effort and MED risk — the pure helpers (`areEqualJsonValues`,
  `mergeStreamEventLogs`, `preserveKnownInitialEvents`) can't be unit-tested
  until extracted. A design/spike worth its own plan after 005 lands.
- **Direction: `get_weather` is a hardcoded stub the agent is told to
  trust** (`agent/tools/get_weather.ts:6-11` returns sunny/72°F always;
  `agent/instructions.md` + `agent/skills/plan_a_trip.md` promise
  weather-driven planning): surface for a maintainer decision (honest-stub
  vs. wire a real API), not a forced plan.
- **Direction: `#evals/*` import map points at a non-existent `evals/` dir**:
  dangling intended-but-undelivered config. Plan 005 deliberately uses a
  standard `vitest` runner instead of coupling to the unbuilt `#evals/*`
  convention. Delete-or-build decision is separate.
- **README "Required env vars" mislabels 3 of 8** (lists both Redis pairs as
  required — they're alternates; hides the `BETTER_AUTH_SECRET` silent
  fallback): LOW-MED docs, S effort. Not planned this batch — coordinate
  with 004 if doing a docs sweep, and note that 002/003 change the behavior
  the README describes.
- **`lib/setup.ts` / `SetupStatus` subsystem is documented at length but
  does not exist in code**: significant stale-docs situation (the docs
  describe a setup-readiness gate that gates nothing). HIGH confidence, S
  effort to rewrite the section. Not planned this batch — it's larger than
  004's three-line fix and benefits from a maintainer's call on whether to
  build the gate (which would actually secure the app — see 002/003) or to
  rewrite the docs to match the simpler reality.
- **`AGENTS.md` is 6 lines and points at `node_modules/eve/dist/docs/public/`
  which doesn't exist in eve@0.16.0**: LOW-MED dx, S effort. Not planned this
  batch; naturally folds into a broader "agent onboarding doc" pass.
- **Mermaid/Shiki pulled into the main bundle unconditionally** (`components/
  chat/markdown.tsx:3-10`): LOW-MED perf, M effort, MED risk (Streamdown
  plugin API may need synchronous registration). Not planned — needs a
  measurement pass first.
