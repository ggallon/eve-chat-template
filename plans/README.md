# Implementation Plans

Generated by the improve skill on 2026-06-27; reconciled 2026-06-28 against
`1b9c9ac`, re-reconciled 2026-07-01 against `ba0cd3a`, again 2026-07-01
against `6f6484f` (the prior reconcile's own commit — no source landed between
the two), again 2026-07-03 against `971fd68` (007 tests landed on `canary`
via `4868aea`; deps bumped in `971fd68` — `eve` 0.16→0.19, `ai` 7.0.3→7.0.14,
`next` 16.2.9→16.2.10; 1 locale-fragile test regression surfaced, plan 009
filed), again 2026-07-08 against `eac0dad` (18 commits landed, including
a full decomposition of `agent-chat.tsx` and a new memory feature; plan 006
substantially refreshed — see the 2026-07-08 reconciliation log below), and
again 2026-07-08 (second pass, same day) against `6454200` (plan 010 landed
and was verified DONE; a dependency bump — `ultracite` 7.9.0→7.9.2 —
shifted plan 006's lint baseline from 100/32 to 101/31; see the newest
reconciliation log below).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Split `components/chat/message.tsx` into a `message/` sub-component module | P2 | M | — | DONE (PR #1, verified 2026-06-28; re-verified 2026-07-08 against `eac0dad`: 9 files present, old file gone) |
| 002  | Fail fast when `BETTER_AUTH_SECRET` is unset | P1 | S | — | DONE (merged via PR #3 / `1b9c9ac`; re-verified 2026-07-08 against `eac0dad`: fallback string gone, `throw` at `lib/auth.ts:15`) |
| 003  | Fail loud when rate-limit Redis is unconfigured | P1 | S | — | DONE (merged via PR #4 / `05b63cb`; re-verified 2026-07-08 against `eac0dad`: `throw new Error(...)` at `lib/rate-limit.ts:52`, no silent `return`) |
| 004  | Re-point stale `components/chat/message.tsx` references in the docs | P2 | S | — | DONE (executed 2026-07-01; re-verified 2026-07-08: stale refs still correctly point to `components/chat/message/index.tsx`, `tool-parts.tsx`, and `tool-group.tsx`) |
| 005  | Add a test runner (vitest) | P1 | S | — | DONE (executed 2026-07-01; re-verified 2026-07-08 against `eac0dad`: `vitest.config.ts` + `test`/`test:run` scripts present) |
| 006  | Repair the broken `pnpm check` lint gate | P2 | **XL** | 005 | **READY — refreshed again 2026-07-08 (second pass)** (baseline now **101 errors/6 warnings/6 infos across 31 files** at `6454200`, up 1 error / down 1 file from the morning's 100/32 at `eac0dad`. Two causes: plan 010 landed and incidentally fixed one finding + shifted line numbers in 3 files; a dependency bump — `ultracite` 7.9.0→7.9.2 — changed rule output on 3 unrelated, unchanged files (`lib/chat/connection.ts` lost a finding, `components/chat/message/index.tsx` gained 3, `components/auth/user-menu.tsx` gained 1). Plan 006 has a new "Drift note (reconcile, 2026-07-08 second pass)" section with the full detail and a caution that `message/index.tsx`'s 3 new findings may indicate real dead code, not just lint noise — read before fixing. Re-audit at Step 1 is still the executor's first move.) |
| 007  | Characterization tests for `lib/chat/*` pure helpers (events, title, limits) | P1 | M | 005 | **DONE — 1 test still fails on `canary` (regression, unfixed)** (landed on `canary` as `4868aea`; 34/35 tests pass; `limits.test.ts:49` still fails at `6454200` with the same locale mismatch, zero drift since `eac0dad` — plan 009 fixes it and has not been executed yet) |
| 008  | Characterization tests for `components/chat/message/tool-status.ts` | P1 | M | 005 | **READY, no drift since `eac0dad`** (dep 005 DONE; `tool-status.ts` byte-identical to `eac0dad` — confirmed via `git diff --stat eac0dad..HEAD`; plan 008's existing drift note about the `default`-case refactor from `971fd68` still applies unchanged) |
| 009  | Fix locale-fragile `limits.test.ts` assertion (007 regression) | P1 | S | 007 | **READY, no drift** (re-confirmed 2026-07-08 second pass: `lib/chat/limits.ts` and `lib/chat/limits.test.ts` are byte-identical to `971fd68`/`eac0dad`; `pnpm test:run` still exits 1 with the exact same failure — 41 passed/1 failed, up from 34/1 since plan 010 added 7 new passing tests; one-line source fix + one-line test fix; see plan 009) |
| 010  | Extract the duplicated "restore draft from sessionStorage" effect into a shared `useRestoredDraft` hook | P3 | S | — | **DONE — verified 2026-07-08 second pass** (landed directly on `canary` as `6454200`; all 7 done criteria re-run and confirmed: `pnpm typecheck` exit 0, `pnpm exec vitest run lib/chat/draft-storage.test.ts` 7/7 pass, `pnpm test:run` 41 passed/1 failed — the one pre-existing `limits.test.ts` failure, exactly as predicted, `grep` shows the `"eve-chat-draft"` literal only in `lib/chat/draft-storage.ts`, scoped `pnpm exec biome check` on the 3 new files reports 0 errors, `git show --stat 6454200` touches exactly the 6 in-scope files and no others. The `advisor/010-extract-restored-draft-hook` branch is now stale — its content is identical to what's on `canary` (`git diff HEAD advisor/010-extract-restored-draft-hook` is empty) — safe to delete.) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **005 split into 005/007/008 (2026-07-01)**: the original plan 005 ("Add a
  test runner and the first batch of characterization tests") bundled runner
  setup with all four characterization test files, including the large
  `tool-status.ts` file. It is now three plans: **005** (runner setup only —
  `vitest`, `vitest.config.ts`, scripts), **007** (`lib/chat/events.test.ts`,
  `lib/chat/title.test.ts`, `lib/chat/limits.test.ts`), and **008**
  (`components/chat/message/tool-status.test.ts`). 007 and 008 both depend on
  005 and are independent of each other — they can run in parallel once 005
  lands. Every other reference to "plan 005" elsewhere in this file that
  predates the split (e.g. in the reconciliation log) refers to the
  pre-split, combined scope; treat it as historical context, not a live
  dependency target.
- **001 → DONE**: executed by PR #1 (`Split chat message into sub-components`).
  The split is in the tree (`components/chat/message/` is a 9-file module);
  plan 004 fixes the stale docs that the split left behind.
- **004 before any doc rewrite** of `docs/how-the-chatbot-works.md`: 004 fixes
  the small stale `message.tsx` references; a broader docs cleanup (the
  "Setup Readiness" / `lib/setup.ts` section describing a subsystem that
  doesn't exist, and the "only Notion" connections-menu drift) is tracked
  as findings but NOT planned yet — coordinate so the large rewrite doesn't
  collide with 004's three-line edits.
- **005 → 006 (REVISED 2026-07-01, supersedes the 2026-06-28 relaxation)**:
  plan 006 originally depended on 005 because its Bucket C rewrote the
  `session-chat-page.tsx` resume-effect hook deps, unsafe to auto-fix before
  the resume behavior was pinned by tests. The 2026-06-28 reconcile claimed
  Bucket C's violations had "silently stopped firing" and relaxed the 005→006
  ordering — **that claim was incorrect** (see Reconciliation log 2026-07-01):
  the violations fire at `ba0cd3a` and always did. Bucket C is real again.
  005 is DONE, so the dependency is satisfied regardless — but 006 must NOT
  be executed as if Bucket C is empty. The refreshed plan 006 restores the
  005 dependency and Bucket C. 005's `pnpm test:run` is the safety net for
  Bucket C; it covers `lib/chat/*` + `tool-status.ts`, NOT the resume effect
  itself, so Bucket C stays manual-judgement per the plan.
- **002 and 003 are DONE** — both security, both S effort, both merged
  (`1b9c9ac` / PR #3 and `05b63cb` / PR #4 respectively). No remaining
  top-priority security item in this plan set.
- **009 fixes a 007 regression (2026-07-03)**: 007's tests landed on `canary`
  (`4868aea`) but `limits.test.ts:49` fails because
  `lib/chat/limits.ts:12` uses `toLocaleString()` without a locale arg and
  the test hardcodes a locale-specific output. 009 pins the locale to
  `"en-US"` in the source and updates the test. 009 should execute BEFORE
  006 — a red `pnpm test:run` gate would block 006's test-dependent buckets.
  009 is independent of 008 (different files).
- **010 is DONE (landed directly on `canary` as `6454200`, verified
  2026-07-08 second-pass reconcile)** — it touched `home-chat-page.tsx`,
  `session-chat-page.tsx`, and `agent-chat-shell.tsx`, all of which plan 006
  also touches for unrelated lint findings. As predicted, 006's line numbers
  in those three files shifted; plan 006's new 2026-07-08-second-pass drift
  note has the updated numbers. No further action needed for 010 itself.
- **Stale branch cleanup (two branches now, not one)**:
  - `advisor/007-test-lib-chat-helpers` (`fb6eb23`) — its content landed on
    `canary` via `4868aea` (with the `useConsistentArrayType` fix applied).
    Safe to delete: `git branch -D advisor/007-test-lib-chat-helpers`. Still
    present as of 2026-07-08 — nobody has run the delete yet.
  - `advisor/010-extract-restored-draft-hook` (`eeefc29`) — its content
    landed on `canary` via `6454200` (`git diff HEAD
    advisor/010-extract-restored-draft-hook` is empty). Safe to delete:
    `git branch -D advisor/010-extract-restored-draft-hook`.
- **Perf findings (F10/F11/F12: `AgentMessage` memoization, per-event
  ownership-SELECT redundancy, O(n²) event-log dedup)** touch the streaming
  hot path in `agent-chat.tsx` and benefit from 007/008's tests existing
  first — not planned in this batch. Note: `agent-chat.tsx` was decomposed
  2026-07-08 (2192→1175 lines, logic split into `agent-chat-shell.tsx` and
  five other new sibling files) — re-locate these three functions before
  acting on this finding; they may have moved.
- **006 substantially rewritten (2026-07-08)**: the `agent-chat.tsx`
  decomposition and the new memory feature roughly doubled the lint-error
  surface (60→100 errors, 18→32 files) between the 2026-07-03 and 2026-07-08
  reconciles. Plan 006 now has a 2026-07-08 drift note with a fresh
  authoritative error table and buckets A-F (A: mechanical, B: `<img>`,
  C: hook-deps/complexity — HIGHEST RISK, D: `dangerouslySetInnerHtml`
  review, E: everything else mechanical, F: new — the `lib/db/schema.ts`
  barrel + `lib/db/relations.ts` namespace-import pair). Execute 009 before
  006 (unchanged advice — a red `pnpm test:run` would block 006's Bucket C
  safety net).

## Reconciliation log (2026-06-28, against `1b9c9ac`)

- **001 verified DONE**: `ls components/chat/message/` shows all 9 files;
  `components/chat/message.tsx` deleted. No regression.
- **002 verified DONE**: `rg "eve-chat-template-unconfigured-secret" lib` →
  no matches; `rg "throw new Error" lib/auth.ts` → one match at `lib/auth.ts:14`.
  Merged via PR #3 (`1b9c9ac`); index previously cited an intermediate branch
  commit, corrected above.
- **003–005 unchanged TODO**: drift check `git diff --stat d1daea6..HEAD` on
  each plan's in-scope files returned empty for all three. Findings re-confirmed
  live: 003's silent `return` at `lib/rate-limit.ts:51-53`; 004's three stale
  `message.tsx` doc refs at lines 41/714/831; 005's runner absent and all four
  target modules present & pure (`lib/chat/events.ts` settled set
  `session.completed`/`session.failed`/`session.waiting`; `lib/chat/title.ts`,
  `lib/chat/limits.ts`, `components/chat/message/tool-status.ts` unchanged).
  `Planned at` SHAs still accurate — no refresh needed.
- **006 DRIFTED (Bucket C gone, Bucket set changed)**: re-ran `pnpm check`
  against `1b9c9ac` — **18 errors**, down from ~21 at planning time. The
  highest-risk subset (Bucket C: 5× `useExhaustiveDependencies` +
  1× `noExcessiveCognitiveComplexity` on `session-chat-page.tsx:73,205,277,308`)
  **no longer fires**. `session-chat-page.tsx` is byte-identical to `d1daea6`
  (`git diff` empty), `biome.jsonc` unchanged, `@biomejs/biome@2.5.1` and
  `ultracite@7.8.3` unchanged — so the rules stopped triggering without any
  config/version bump. Cause unknown; flagging as a separate item below.
  Consequence: **006's Bucket C is empty**; the 005 dependency was for Bucket C
  only, so 005→006 is relaxed (see Dependency notes). 006's Buckets A/B/D
  largely still apply, but the current 18-error set also differs from 006's
  cited set in ways the executor must re-derive at Step 1:
  - **New errors not anticipated by 006**: `components/chat/composer.tsx`
    (3: `useAriaPropsSupportedByRole`, `noNoninteractiveTabindex`,
    `noNestedTernary`), `agent-chat-shell.tsx:100` (`noDocumentCookie`),
    `agent-chat-shell.tsx:404` (`noDangerouslySetInnerHtml` — 006 listed
    agent-chat-shell.tsx but only for 3 a11y errors at line 355, not this),
    `sign-in-button.tsx:65` (`useOptionalChain` — 006 listed sign-in-button.tsx
    in-scope but didn't cite this rule).
  - **Errors 006 cited that still match**: `auth-display.tsx:36` +
    `app/layout.tsx:103` (`noDangerouslySetInnerHtml`, Bucket D), `user-menu.tsx:92`
    (`noImgElement`+`useImageSize`, Bucket B), `app/api/auth/[...all]/route.ts`
    (`useAwait`×2, `noUnusedVariables`, `useOptionalChain` — note 006 cited
    the route's `useOptionalChain` but Step 1 now shows it on
    `sign-in-button.tsx:65` instead; re-read both), `home-chat-page.tsx:61,65`
    (`useExhaustiveDependencies` — STILL fire, just 2 not 5; and
    `home-chat-page.tsx:138` `noEmptyBlockStatements`), `agent-chat-shell.tsx:355`
    (3 a11y), `agent/tools/get_weather.ts:9` (`useAwait`).
  - **Action for 006**: the plan's Step 1 ("re-audit the current error list,
    trust Step 1 over the plan's numbers") is exactly the right instruction and
    survives the drift. But the plan's Step 5 (Bucket C) and the 005 dependency
    are stale. **Refresh 006 in place** before executing: drop Step 5, drop the
    005 dependency in the Status block, and add the new files
    (`composer.tsx`, the `agent-chat-shell.tsx:100/404` lines,
    `sign-in-button.tsx:65`) to the in-scope list with the same bucketing
    instinct (a11y/`noDocumentCookie`/`noNestedTernary` are low-risk; the new
    `noDangerouslySetInnerHtml` at `agent-chat-shell.tsx:404` goes into
    Bucket D and needs the same static-vs-runtime review).
- **New finding surfaced during reconcile**: the silent disappearance of
  `session-chat-page.tsx`'s resume-effect lint violations, with no config or
  version change, is itself suspicious. Either (a) an ultracite rule default
  changed out-of-band, (b) a transitive biome plugin update shifted, or
  (c) the resume effect was genuinely refactored in a way `git diff` against
  `d1daea6` doesn't capture (e.g. an upstream `eve/react` type change altered
  what `useExhaustiveDependencies` sees). Not blocking, but worth a
  `pnpm update`/lockfile-diff check before trusting the gate. Filed below in
  "considered and rejected" as a *watch item*, not a new plan.

## Reconciliation log (2026-07-01, against `ba0cd3a`)

- **001 verified DONE** (still): `ls components/chat/message/` → 9 files;
  `components/chat/message.tsx` absent.
- **002 verified DONE** (still): `throw` at `lib/auth.ts:14-15`; no
  `eve-chat-template-unconfigured-secret` fallback string anywhere in `lib/`.
- **003 verified DONE + merged**: the prior index note ("staged on
  `advisor/003-fail-loud-rate-limit` — commit pending SSH signing-key
  passphrase") was stale. The change is on `canary` as PR #4 (`05b63cb`).
  `lib/rate-limit.ts:51-55` throws; no silent `return`. Corrected in the
  status table above.
- **004 verified DONE** (still): `docs/how-the-chatbot-works.md` lines 41/714/
  831 now reference `components/chat/message/index.tsx`, `tool-parts.tsx`,
  `tool-group.tsx`.
- **005 verified DONE** (still): `pnpm test:run` → exit 0 ("No test files
  found", `passWithNoTests`); `pnpm typecheck` → exit 0; `vitest.config.ts`
  present; `test`/`test:run` scripts present.
- **006 DRIFTED AGAIN — the 2026-06-28 "18 errors / Bucket C empty" note was
  incorrect.** Re-ran `pnpm check --max-diagnostics=300` on current HEAD and
  got **60 errors + 3 warnings + 7 infos across 20 files** (70 diagnostics
  total). To rule out a real between-commit drift I checked out `d1daea6` and
  `1b9c9ac` in disposable git worktrees, symlinked the current tree's
  `node_modules/ultracite` + `node_modules/@biomejs/biome` (same pinned
  versions: `@biomejs/biome@2.5.1`, `ultracite@7.8.3`, `biome.jsonc`
  unchanged, all in-scope files byte-identical across the three commits), and
  ran `biome check` directly: **all three commits yield the same 60/3/7
  count.** So the "18 errors" figure and the "session-chat-page.tsx
  violations silently stopped firing / Bucket C empty" narrative in the
  2026-06-28 log were wrong — most likely that reconcile never actually
  re-ran `pnpm check` with ultracite's extends resolving (e.g. ran biome
  against a worktree where `ultracite/biome/core` couldn't resolve, silently
  degrading the rule set) and confabulated the count from there. The
  `useExhaustiveDependencies` violations at `session-chat-page.tsx:73,277,
  308,316` and `noEmptyBlockStatements` at `:139` all fire. **Bucket C is
  real; the 005 dependency (now DONE) is required again.** Plan 006 has been
  refreshed in place: the 2026-06-28 drift note is replaced with a corrected
  2026-07-01 note, Bucket C is restored, the 005 dependency is restored, the
  "Current state" error list is replaced with the real 60-error/20-file set,
  the in-scope file list is expanded, and the bucket map is rewritten. Effort
  is re-estimated M→L (the set is ~3× larger than planned and spans 11 files
  the original plan never listed, including `agent-chat.tsx` with 17). The
  structural approach (Step 1 re-audit, bucket by risk, no global `pnpm fix`,
  manual judgement on resume-effect hook deps) is still sound.
- **007 & 008 — no drift, dependency satisfied, now READY.** Drift check
  `git diff --stat d1daea6..HEAD -- lib/chat/ components/chat/message/`
  → empty for both. Target modules re-confirmed pure and present:
  `lib/chat/events.ts` settled set is `authorization.required` +
  `session.completed`/`session.failed`/`session.waiting` (the plan's hedged
  "plus one more — read the file" covers this); `lib/chat/title.ts` unchanged
  (`>72` → `slice(0,69).trimEnd() + "..."`); `lib/chat/limits.ts` unchanged
  but note it calls `message.trim()` before `Array.from(message).length`
  (code-point length, not UTF-16) — a small 007 drift note was added so the
  characterization tests cover the trim + emoji-length edge cases;
  `tool-status.ts` exports unchanged. Both plans executable as-is right now.
- **Watch items re-verified at `ba0cd3a`** (still valid, none became plans):
  no `headers()` in `next.config.ts` (CSP); `incr`/`expire` race in
  `lib/rate-limit.ts:60-64` still present; `@shikijs/*` + `shiki` v4 still
  declared as direct deps and unused; `lib/setup.ts` / `SetupStatus` still
  does not exist (stale-docs); `AGENTS.md` grew 6→14 lines but still points at
  `node_modules/eve/dist/docs/public/` which does NOT exist in `eve@0.16.0`;
  `#evals/*` import map (`package.json:8`) still points at an empty `evals/`
  dir; README lines 63-66 still list both Redis pairs as "required" (they're
  alternates); `agent-chat.tsx` still ~2192 LOC (lint errors fire at
  `:1895`); `get_weather.ts` still a hardcoded `Sunny`/`72°F` stub. One watch
  item CHANGED — see the corrected note in "Findings considered and rejected".

## Reconciliation log (2026-07-01, against `6f6484f`)

No new source landed since the prior reconcile (`ba0cd3a` → `6f6484f` is the
prior reconcile's own commit; `git diff --stat 6f6484f..HEAD -- app/ components/
lib/ agent/` is empty). So every watch item above is unchanged and was not
re-walked. This pass focused on DONE spot-checks, the 007 merge state, and the
006/008 drift checks.

- **001–005 verified DONE (still)**: 001 `components/chat/message/` 9 files,
  `message.tsx` absent; 002 `throw` at `lib/auth.ts:14-15`, no fallback string;
  003 `throw` at `lib/rate-limit.ts:51-55`, no silent `return`; 004 doc refs at
  `docs/how-the-chatbot-works.md` 41/714/831 re-pointed; 005 `vitest.config.ts`
  + `test`/`test:run` scripts present. Baseline on `canary`@`6f6484f`:
  `pnpm typecheck` exit 0; `pnpm test:run` exit 0 ("No test files found" — 007
  not merged yet); `pnpm check` exit 1 with the 60-error/20-file set (006's
  target, unchanged).
- **007 DONE but branch unmerged + prior "zero lint errors" claim was wrong.**
  Re-verified in the live worktree `/private/tmp/eve-exec-007` (`fb6eb23`):
  `pnpm test:run` → 35/35 pass across `events.test.ts` (17), `title.test.ts`
  (10), `limits.test.ts` (8); `pnpm typecheck` exit 0. BUT `biome check` on the
  3 test files reports **4 errors**: `useConsistentArrayType` at
  `events.test.ts:42,176` (auto-fixable — `ReadonlyArray<T>` → `readonly T[]`)
  and `useTopLevelRegex` at `limits.test.ts:49,52` (regex literals inside
  `it` blocks — move to module top level). The prior reconcile's "zero new
  lint errors in test files" claim was inaccurate. Branch
  `advisor/007-test-lib-chat-helpers` adds only the 3 test files (no conflicts,
  `git merge-tree` clean) but is NOT on `canary`. **Action for the user:**
  `git merge advisor/007-test-lib-chat-helpers` to land it; the 4 test-file
  lint errors then join 006's set (60 → 64), all low-risk buckets, and 006's
  Step 1 re-audit already handles the re-count. No plan rewrite needed.
- **006 READY (no drift, already refreshed)**: in-scope files unchanged since
  `6f6484f`; the plan was refreshed in place during the prior reconcile (Bucket
  C restored, 60-error set, 005 dep satisfied). Status string corrected from
  "DRIFTED AGAIN" to "READY" above — the drift was already reconciled into the
  plan text; the old label made it sound un-executable. Executable as-is.
- **008 READY (no drift)**: `components/chat/message/` unchanged since
  `d1daea6`; dep 005 DONE; target module pure. Executable as-is right now,
  independent of 007's merge state (different directory).

## Reconciliation log (2026-07-03, against `971fd68`)

Two commits landed since the prior reconcile (`6f6484f`):
- `4868aea` — "Plan 007: Characterization tests for pure helpers": the 3
  test files from `advisor/007-test-lib-chat-helpers` landed on `canary`
  (not via `git merge` — the branch tip `fb6eb23` was never merged; content
  was committed directly with the 2× `useConsistentArrayType` auto-fix
  applied to `events.test.ts`). The branch `advisor/007-test-lib-chat-helpers`
  is now stale (content is on `canary`) and can be deleted.
- `971fd68` — "Update dependencies": `eve` 0.16.0→0.19.0, `ai` 7.0.3→7.0.14,
  `better-auth` 1.6.20→1.6.23, `next` 16.2.9→16.2.10, `@vercel/connect`
  0.2.10→0.3.2, `lucide-react` 1.21.0→1.22.0, `radix-ui` 1.6.0→1.6.1,
  `microsandbox` 0.5.10→0.6.1, `@tailwindcss/postcss`/`tailwindcss`
  4.3.1→4.3.2, `@typescript/native-preview` bumped. `ultracite` 7.8.3 and
  `@biomejs/biome` 2.5.1 unchanged. `agent/sandbox/sandbox.ts` lost
  `runtime: "node24"` (microsandbox 0.6.1 API change — not plan-relevant).

- **001–005 verified DONE (still)** on `971fd68`: 001 `components/chat/message/`
  9 files, `message.tsx` absent; 002 `throw` at `lib/auth.ts:14`, no fallback
  string; 003 `throw` at `lib/rate-limit.ts:52`, no silent `return`; 004 doc
  refs at `docs/how-the-chatbot-works.md` 41/714/831 re-pointed; 005
  `vitest.config.ts` + `test`/`test:run` scripts present.

- **Baseline at `971fd68`**: `pnpm typecheck` exit 0; `pnpm check` exit 1
  with **60 errors + 3 warnings + 7 infos across 18 files** (same 60-error
  count as `ba0cd3a`/`6f6484f` — the dep bump did NOT change the lint set;
  the file count dropped 20→18 but the error count is identical, some files
  consolidated). `pnpm test:run` **exit 1** — 34/35 pass, 1 fails
  (`limits.test.ts:49`, the locale-fragile assertion — see plan 009).

- **007 DONE but 1 test regresses on `canary`** — `pnpm test:run` reports:
  `FAIL lib/chat/limits.test.ts > ... > throws an error whose message names
  the formatted limit and the character budget` — expected
  `'Messages must be 8\u202f000 character…'` (narrow no-break space) but got
  `'Messages must be 8,000 characters or …'` (comma). Root cause:
  `lib/chat/limits.ts:12` calls `MAX_CHAT_MESSAGE_CHARS.toLocaleString()`
  with no locale arg → output is runtime-locale-dependent. The 007 executor
  verified in an environment that produced `\u202f` and hardcoded it; the
  current environment produces `,`. **Plan 009 filed** to pin
  `toLocaleString("en-US")` in the source and update the test assertion.
  Also: the prior reconcile's claim of "2× `useTopLevelRegex` at
  `limits.test.ts:49,52`" was a **mis-attribution** — the file has no regex
  literals (line 49 is a `.toThrow(string)` call); the only real 007
  test-file lint errors were the 2× `useConsistentArrayType` in
  `events.test.ts`, which were fixed on landing. So 006's expected count
  stays at 60, not 64.

- **006 READY (no refresh needed)**: 60 errors re-confirmed on `971fd68`.
  All current error-set files are already in 006's in-scope list (verified
  by grep: `lib/auth-url.ts`, `lib/db/client.ts`, `components/chat/sidebar.tsx`,
  `components/chat/message/use-streaming-text.ts`, `index.tsx`, `tool-group.tsx`
  all appear in the plan). The dep bump didn't shift the rule set
  (`ultracite`/`biome` unchanged). Step 1 re-audit remains the source of
  truth at execution time. Executable as-is.

- **008 READY (no drift)**: `git diff --stat d1daea6..HEAD --
  components/chat/message/` → empty. Dep 005 DONE. Target module
  `tool-status.ts` unchanged and pure. Executable as-is, independent of 009
  (different directory, different concern).

- **Watch items re-verified at `971fd68`**:
  - `eve` is now 0.19.0 (was 0.16.0) but
    `node_modules/eve/dist/docs/public/` **still does not exist** — the
    `AGENTS.md` pointer remains stale.
  - `agent/sandbox/sandbox.ts` no longer specifies `runtime: "node24"`
    (microsandbox 0.6.1) — not a finding, just observed.
  - The `postcss` transitive vuln watch item may be resolved or changed by
    the `next` 16.2.10 bump — not re-audited this pass (would need
    `pnpm audit`).
  - All other prior watch items (incr/expire race, dead shiki deps,
    `lib/setup.ts` stale-docs, `#evals/*` import map, README env-var
    mislabeling, `agent-chat.tsx` god-module, Mermaid/Shiki bundle) were not
    re-walked — no source in those areas changed since `6f6484f`.

## Reconciliation log (2026-07-08, against `eac0dad`)

18 commits landed since the prior reconcile (`971fd68`), the largest single
batch since planning began: `98 files changed, 7628 insertions(+), 3281
deletions(-)` (`git diff --stat 971fd68..eac0dad`). Highlights: "Refactor the
agent-chat.tsx file into several files" (2192→1175 lines, split into
`agent-chat-shell.tsx`, `agent-chat-bootstrap-sync.tsx`,
`agent-chat-route-sync.tsx`, `chat-shell-context.tsx`, `controller.ts`,
`error-toast.tsx`, `composer-footer-controls.tsx`, `types.ts`), "Add memory
tool feature" (`lib/memory/*`, `agent/tools/save_memory.ts`, a new
`lib/db/schema/memory.ts`), a `lib/chat/*` module split (`connection.ts`,
`error.ts`, `json-utils.ts`, `message.ts`, `session.ts`, `stream.ts` — all
new), `lib/db/schema.ts` split into `schema/{auth,chat,memory,profile}.ts`
with a barrel re-export, a shadcn/ui component refresh, and dependency
bumps (`eve` 0.19.0→0.22.0, `ai` 7.0.14→7.0.16, `better-auth`
1.6.23→1.7.0-rc.1, `next` unchanged at 16.2.10, `drizzle-orm` bumped to
1.0.0-rc.4).

**Baseline at `eac0dad`**: `pnpm typecheck` exit 0. `pnpm test:run` exit 1 —
34/35 pass, `limits.test.ts:49` fails with the exact same locale mismatch as
`971fd68` (plan 009, unexecuted). `pnpm check --max-diagnostics=300` exit 1
with **100 errors + 6 warnings + 6 infos across 32 files**, up from 60/3/7
across 18 files at `971fd68` — a real drift (confirmed via the file diff
above), not a measurement artifact.

- **001-005 verified DONE (still)**, re-confirmed against `eac0dad`:
  001 `components/chat/message/` 9 files, `message.tsx` absent; 002 `throw`
  at `lib/auth.ts:15` (line shifted by 1, content unchanged), no fallback
  string; 003 `throw` at `lib/rate-limit.ts:52`, no silent `return`; 004 doc
  refs at `docs/how-the-chatbot-works.md` correctly re-pointed (re-grepped,
  unchanged); 005 `vitest.config.ts` + `test`/`test:run` scripts present,
  `pnpm test:run` runs 3 test files (35 tests).
- **006 substantially drifted — plan rewritten in place, not just refreshed.**
  Unlike the 2026-07-01/07-03 drifts (same files, shifted line numbers), this
  drift changed the codebase's *structure*: `agent-chat.tsx` lost ~1000
  lines to new sibling files, and the new memory feature added 6 previously
  nonexistent files with their own lint findings. Re-ran `pnpm check
  --max-diagnostics=300` and built a complete per-file/per-rule table (see
  plan 006's "Drift note (2026-07-08)"). The historical "Bucket C" (resume
  effect hook-deps, the highest-risk category) is still real and still maps
  cleanly to its successor: `session-chat-page.tsx`'s effect (now at
  `:65,269,308`, was `:73,277,308,316`) plus a NEW second high-risk effect in
  `agent-chat.tsx:751` (the chat-switch/title/known-events effect, successor
  to the pre-refactor `agent-chat.tsx:1349`) plus `home-chat-page.tsx:53,57`.
  Added a new "Bucket F" for a linked pair the old plan never saw:
  `lib/db/schema.ts`'s `noBarrelFile` + `lib/db/relations.ts`'s
  `noNamespaceImport` — traced the barrel's only consumer (grep confirms
  `lib/db/relations.ts` is the sole importer of `./schema`; every other call
  site already imports from the sub-modules directly) so the executor doesn't
  have to re-derive that. Effort re-estimated L→XL. Plan is READY (005 dep
  satisfied) but should be treated as effectively a new plan — re-read it in
  full before executing, the old sections are marked stale in-place rather
  than deleted.
- **007 confirmed unchanged**: still DONE-with-regression on `canary`;
  `limits.test.ts:49` fails identically to the `971fd68` reconcile. Plan 009
  has not been executed.
- **008 drift confirmed cosmetic, not behavioral**: `tool-status.ts` changed
  11 lines (`getToolStatus`'s switch: 4 explicit `"running"` cases →
  commented out + a `default: return "running"`). Read both versions
  (`git show 971fd68:...` vs current) — every state maps to the same
  `ToolStatus` before and after. Added a drift note to plan 008 documenting
  this; no plan changes needed, still READY.
- **009 confirmed zero drift**: `git diff --stat 971fd68..eac0dad --
  lib/chat/limits.ts lib/chat/limits.test.ts` is empty. Still READY exactly
  as written.
- **Watch items re-verified at `eac0dad`**: `eve` now 0.22.0 (was 0.19.0),
  `node_modules/eve/dist/docs/public/` **still does not exist** — `AGENTS.md`
  (which grew 14→34 lines this batch, `+20` per `git diff --stat`) still
  points at it; still stale. `lib/rate-limit.ts:60,63`'s `incr`/`expire` race
  still present, unchanged. Dead `@shikijs/*`/`shiki` v4 direct deps still
  declared, unchanged. `#evals/*` import map (`package.json:8`) still points
  at a nonexistent `evals/` dir. `lib/setup.ts` still does not exist (the
  stale-docs finding is unchanged). Not re-walked this pass: README env-var
  labeling, Mermaid/Shiki bundle size, `postcss` transitive vuln (would need
  `pnpm audit`, not run — read-only reconcile budget).
- **New observation, not yet a finding**: the new `lib/chat/*` files
  (`connection.ts`, `error.ts`, `json-utils.ts`, `message.ts`, `session.ts`,
  `stream.ts`) are plausible candidates for a 007-successor characterization-
  test plan — some look pure (`error.ts`'s message extraction), others do
  real fetch/stream I/O (`stream.ts`, `session.ts`) and would need a fetch
  mock, not a pure-function harness like 007/008. Not investigated deeply
  enough to file a plan (`reconcile` doesn't run a fresh audit); flagging for
  the next full `/improve` pass rather than guessing at scope here.
- **Not re-run this pass**: `pnpm audit` (dependency vuln scan) and any
  visual/behavioral check of the new memory feature or the `agent-chat.tsx`
  decomposition beyond `pnpm typecheck`/`pnpm test:run`/`pnpm check` passing.
  A full `/improve` audit would be worthwhile given the scale of this batch
  (a new feature + a large refactor landed together) — reconcile only keeps
  existing plans honest, it doesn't replace a fresh audit.

## Reconciliation log (2026-07-08 second pass, against `6454200`)

Four commits landed since the prior reconcile (`eac0dad`): `6208494`
("Reconcile plans" — the prior reconcile's own commit), `437f112` ("Update
dependencies" — `ai` 7.0.16→7.0.17, `@typescript/native-preview` bumped,
`ultracite` 7.9.0→7.9.2, `vitest` 4.1.9→4.1.10; `@biomejs/biome` unchanged at
2.5.2), `de1bda4` ("Add Plan 010" — the plan file itself), and `6454200`
("Plan 010 extract restored draft hook" — the plan's execution, landed
directly on `canary`, not via a merged PR). Net source diff since `eac0dad`:
`git diff --stat eac0dad..HEAD -- app/ components/ lib/ agent/` touches
exactly 6 files (3 modified: `agent-chat-shell.tsx`, `home-chat-page.tsx`,
`session-chat-page.tsx`; 3 new: `use-restored-draft.ts`, `draft-storage.ts`,
`draft-storage.test.ts`) — all plan 010's Step 1-6 changes, nothing else.

- **001-005 verified DONE (still)**, re-confirmed against `6454200`: 001
  `components/chat/message/` 9 files, `message.tsx` absent; 002 `throw` at
  `lib/auth.ts:15`; 003 `throw` at `lib/rate-limit.ts:52`; 004 doc refs at
  `docs/how-the-chatbot-works.md` 41/714/831 correctly re-pointed; 005
  `vitest.config.ts` + `test`/`test:run` scripts present.
- **010 verified DONE** — re-ran every done criterion from
  `plans/010-extract-restored-draft-hook.md` against `6454200` fresh (not
  trusting the executor's own report, since none was available — this plan
  was executed directly rather than through the `execute` dispatch flow):
  `pnpm typecheck` exit 0; `pnpm exec vitest run lib/chat/draft-storage.test.ts`
  → 7/7 pass; `pnpm test:run` → 41 passed/1 failed (the 1 failure is
  `limits.test.ts:49`, the pre-existing locale regression plan 009 fixes —
  exactly the count plan 010 predicted for "before 009 lands"); `grep -rn
  --exclude='*.test.ts' "eve-chat-draft" app/ components/ lib/` → exactly one
  match, the definition in `lib/chat/draft-storage.ts`; `pnpm exec biome
  check lib/chat/draft-storage.ts lib/chat/draft-storage.test.ts
  app/_components/use-restored-draft.ts` → 0 errors; `git show --stat
  6454200` touches exactly the 6 in-scope files listed in the plan's Scope
  section, nothing else (verified by reading the full diff, not just the
  stat — every hunk traces to a specific plan step: Step 1's storage module,
  Step 3's hook, Steps 4-6's three call-site replacements). No scope
  violations, no undocumented deviations. The landed test file has 2 extra
  helper methods (`clear`, `key`, a `length` getter) on the fake
  `Storage` mock beyond the plan's exact snippet — a harmless superset that
  doesn't change what's asserted; not a deviation worth flagging.
- **006 refreshed again (small drift, not a rewrite)**: baseline moved from
  100 errors/32 files (`eac0dad`) to **101 errors/31 files** (`6454200`).
  Root-caused to two independent, unrelated changes (full detail now lives
  in plan 006's own "Drift note (reconcile, 2026-07-08 second pass)"
  section, not duplicated here): plan 010's edits shifted line numbers in 3
  files and incidentally fixed one `noEmptyBlockStatements` finding in
  `session-chat-page.tsx` (removed a `catch {}` block); separately, the
  `ultracite` 7.9.0→7.9.2 bump changed rule output on 3 files that have
  **not** changed since `eac0dad` (`lib/chat/connection.ts` lost a finding,
  `components/chat/message/index.tsx` gained 3 — flagged as worth
  investigating for a real dead-code bug, not blind-fixing — and
  `components/auth/user-menu.tsx` gained 1). The bucket-map PROCESS and risk
  assessment (Bucket C highest-risk, 005 dependency) are unaffected. Still
  READY.
- **007, 008, 009 confirmed zero drift**: `git diff --stat eac0dad..HEAD --
  lib/chat/events.ts lib/chat/events.test.ts lib/chat/title.ts
  lib/chat/title.test.ts lib/chat/limits.ts lib/chat/limits.test.ts
  components/chat/message/tool-status.ts` is empty. All three plans
  executable exactly as written; 007's `canary` regression is unchanged.
- **Watch items re-verified at `6454200`**: `eve` still 0.22.0 (untouched by
  this dependency bump); `node_modules/eve/dist/docs/public/` still does not
  exist; `AGENTS.md` still 34 lines, still points at the missing path;
  `lib/rate-limit.ts:60-63`'s `incr`/`expire` race still present, unchanged;
  dead `@shikijs/*`/`shiki` v4 deps still declared; `#evals/*` import map
  still points at a nonexistent dir; `lib/setup.ts` still does not exist.
  Not re-walked this pass (unchanged from `eac0dad`, no reason to suspect
  drift): README env-var labeling, Mermaid/Shiki bundle size, `pnpm audit`.
- **Two stale branches identified, neither deleted (reconcile is read-only
  on the git tree)**: `advisor/007-test-lib-chat-helpers` (flagged since
  2026-07-03, still not deleted) and the newly-stale
  `advisor/010-extract-restored-draft-hook` (content landed via `6454200`,
  `git diff HEAD advisor/010-extract-restored-draft-hook` is empty). Both
  are safe one-line `git branch -D` deletes for the user to run.

## Notes

- This set was produced by a full audit (Phase 1 recon + parallel category
  audits + vetting against the cited code). Planned at commit `d1daea6` on
  the `canary` branch; reconciled against `1b9c9ac` on 2026-06-28,
  re-reconciled against `ba0cd3a` on 2026-07-01, and again against `6f6484f`
  on 2026-07-01.
- Repo verification baseline at planning time (`d1daea6`): `pnpm typecheck`
  exits 0; `pnpm check` exits 1 with ~21 errors (pre-existing — that's plan
  006); no `pnpm test` runner exists (plan 005 adds it).
- **Baseline at `1b9c9ac` (2026-06-28)**: `pnpm typecheck` exits 0; the
  2026-06-28 log recorded `pnpm check` at "18 errors" — **this figure was
  incorrect** (see 2026-07-01 log); the real count at `1b9c9ac` with the
  pinned toolchain is 60 errors. No test runner yet at that point.
- **Current baseline at `6f6484f` (2026-07-01, second reconcile)**: `pnpm
  typecheck` exit 0; `pnpm test:run` exit 0 ("No test files found" — 007's
  branch is DONE but unmerged, so canary still has no test files); `pnpm check`
  exit 1 with the same **60 errors + 3 warnings + 7 infos across 20 files** as
  `ba0cd3a` (no source drift). Once `advisor/007-test-lib-chat-helpers` is
  merged, `pnpm test:run` reports 35 tests and `pnpm check` rises to 64 errors
  (the 4 test-file errors are listed in 007's status row). 006's Step 1
  re-audit remains the source of truth at execution time.
- **Baseline at `971fd68` (2026-07-03, third reconcile)**: `pnpm typecheck`
  exit 0; `pnpm test:run` **exit 1** — 34/35 pass, 1 fails
  (`limits.test.ts:49` locale-fragile assertion — plan 009 fixes it);
  `pnpm check` exit 1 with **60 errors + 3 warnings + 7 infos across 18
  files** (same 60-error count as prior; file count dropped 20→18, some
  consolidated; the 007 test files contribute zero lint errors — the 2×
  `useConsistentArrayType` were fixed on landing, and the 2×
  `useTopLevelRegex` were a prior-reconcile mis-attribution).
- **Baseline at `eac0dad` (2026-07-08, fourth reconcile)**: `pnpm typecheck`
  exit 0; `pnpm test:run` **exit 1** — 34/35 pass, `limits.test.ts:49` fails
  identically to `971fd68` (plan 009 still unexecuted); `pnpm check` exit 1
  with **100 errors + 6 warnings + 6 infos across 32 files** — a real ~1.7×
  drift from `971fd68`'s 60/3/7/18, driven by the `agent-chat.tsx`
  decomposition (new `agent-chat-shell.tsx` alone: 14 diagnostics) and the
  new memory feature (`lib/chat/*`, `lib/memory/*`, `lib/db/schema/*`: ~15
  diagnostics across 8 new files). Plan 006 was rewritten in place with the
  full current table; see the 2026-07-08 reconciliation log above for the
  file-by-file breakdown.
- **Baseline at `6454200` (2026-07-08 second pass, fifth reconcile)**: `pnpm
  typecheck` exit 0; `pnpm test:run` **exit 1** — **41 passed, 1 failed**
  (up from 34/1 — plan 010 added 7 new passing tests in
  `lib/chat/draft-storage.test.ts`; the 1 failure is still
  `limits.test.ts:49`, plan 009 unexecuted); `pnpm check` exit 1 with **101
  errors + 6 warnings + 6 infos across 31 files** — a small further drift
  from `eac0dad`'s 100/6/6/32, caused by plan 010 landing (fixed one
  finding, shifted line numbers in 3 files) plus a dependency bump
  (`ultracite` 7.9.0→7.9.2, changed rule output on 3 unrelated files). Plan
  006 got a small addendum, not a rewrite; see the 2026-07-08-second-pass
  reconciliation log above.
- All plans inline the conventions, excerpts, and commands the executor
  needs; no plan references "the audit" or "the discussion."

## Findings considered and rejected (so nobody re-audits them)

- **CORRECTED 2026-07-03: the 2026-07-01 claim of "2× `useTopLevelRegex` at
  `limits.test.ts:49,52`" was a mis-attribution.** The file has no regex
  literals — line 49 is a `.toThrow(string)` call. The only real 007
  test-file lint errors were the 2× `useConsistentArrayType` in
  `events.test.ts:42,176`, which were fixed when 007 landed on `canary`
  (`4868aea`). Net effect: 006's expected lint count stays at 60, not 64;
  the 007 test files contribute zero lint errors on `canary`.

- **WATCH (CORRECTED 2026-07-01): the 2026-06-28 claim that
  `session-chat-page.tsx`'s resume-effect lint violations "silently stopped
  firing" was wrong.** They fire at `ba0cd3a` (`useExhaustiveDependencies` at
  `:73,:277,:308,:316` and `noEmptyBlockStatements` at `:139`), and
  verification across `d1daea6`/`1b9c9ac`/`ba0cd3a` with the pinned toolchain
  shows the same 60-error count at all three. The "silent disappearance" was
  a measurement artifact of the 2026-06-28 reconcile (likely `ultracite/biome`
  extends not resolving in the worktree it used), not a real rule relaxation.
  Net effect: the resume-effect code IS currently lint-flagged, plan 006's
  Bucket C is real and must be handled (manual judgement, 005 as safety net),
  and the "characterize the resume effect" follow-up (a 007/008 successor that
  adds tests for the resume path itself) remains valuable — arguably more so,
  since Bucket C will touch that untested code.

- **CSP / security headers** (next.config.ts has no `headers()`): MED
  confidence, could not confirm whether Next.js 16 already ships a baseline
  CSP; the chat surface is same-origin. Not worth a plan at this depth —
  revisit if Streamdown sanitization is ever in question.
- **`incr`/`expire` race in `lib/rate-limit.ts`** (a crash between `incr`
  and `expire` leaves a rate-limit key with no TTL): real but separate from
  plan 003; file as its own finding later, don't fold into 003.
- **postcss transitive `<8.5.10` (GHSA-qx2v-qp2m-jg93, moderate XSS)** via
  next 16.2.9's pinned postcss: low-impact (build-time), fixable via
  `pnpm.overrides`, but needs a `next build` + visual smoke to verify. Not
  planned this batch — a one-line `pnpm.overrides` is a small follow-up.
- **Dead shiki direct deps** (`@shikijs/*` + `shiki` v4 declared, never
  imported; streamdown pulls v3 transitively): LOW impact, S effort. Not
  planned this batch but trivially removable — a quick win for the next run.
- **Three divergent "owned chat" guard patterns** in `lib/db/queries.ts`
  (4 sites, drift between throw and return-null): LOW-MED tech-debt, S
  refactor. Not planned; fold into the eventual `lib/db/queries.ts`
  test+refactor pass.
- **`agent-chat.tsx` 2192-LOC god-module**: real, HIGH confidence, but L
  effort and MED risk — the pure helpers (`areEqualJsonValues`,
  `mergeStreamEventLogs`, `preserveKnownInitialEvents`) can't be unit-tested
  until extracted. A design/spike worth its own plan after 005 lands.
- **Direction: `get_weather` is a hardcoded stub the agent is told to
  trust** (`agent/tools/get_weather.ts:6-11` returns sunny/72°F always;
  `agent/instructions.md` + `agent/skills/plan_a_trip.md` promise
  weather-driven planning): surface for a maintainer decision (honest-stub
  vs. wire a real API), not a forced plan.
- **Direction: `#evals/*` import map points at a non-existent `evals/` dir**:
  dangling intended-but-undelivered config. Plan 005 deliberately uses a
  standard `vitest` runner instead of coupling to the unbuilt `#evals/*`
  convention. Delete-or-build decision is separate.
- **README "Required env vars" mislabels 3 of 8** (lists both Redis pairs as
  required — they're alternates; hides the `BETTER_AUTH_SECRET` silent
  fallback): LOW-MED docs, S effort. Not planned this batch — coordinate
  with 004 if doing a docs sweep, and note that 002/003 change the behavior
  the README describes.
- **`lib/setup.ts` / `SetupStatus` subsystem is documented at length but
  does not exist in code**: significant stale-docs situation (the docs
  describe a setup-readiness gate that gates nothing). HIGH confidence, S
  effort to rewrite the section. Not planned this batch — it's larger than
  004's three-line fix and benefits from a maintainer's call on whether to
  build the gate (which would actually secure the app — see 002/003) or to
  rewrite the docs to match the simpler reality.
- **`AGENTS.md` is 6 lines and points at `node_modules/eve/dist/docs/public/`
  which doesn't exist in eve@0.16.0**: LOW-MED dx, S effort. Not planned this
  batch; naturally folds into a broader "agent onboarding doc" pass.
- **Mermaid/Shiki pulled into the main bundle unconditionally** (`components/
  chat/markdown.tsx:3-10`): LOW-MED perf, M effort, MED risk (Streamdown
  plugin API may need synchronous registration). Not planned — needs a
  measurement pass first.
